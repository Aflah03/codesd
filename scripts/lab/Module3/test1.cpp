#include<iostream>
#include<bits/stdc++.h>
using namespace std;
void twoSum(vector<int>& nums, int target){

}

int main(){
    unordered_map<int , string> mymap;
    mymap.insert({1,"Aflah"});
    mymap.insert({5,"shameem"});
    mymap.insert({7,"swalih"});
    mymap.insert({7,"swalih"});
    mymap.insert({3,"fari"});
    
    for(auto i: mymap){
        cout << i.first << " " << i.second<< endl; 
    }
}